import { Homepage } from './Home';
import { Services } from './Services';
import { Navigation } from './Navigation';

export const en = {
  translations: {
    Homepage,
    Services,
    Navigation
  }
};